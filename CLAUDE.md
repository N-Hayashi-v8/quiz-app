# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際のClaude Code(claude.ai/code)へのガイダンスです。

## プロジェクト概要

- **プロジェクト名**: quiz-app
- **内容**: 一般常識クイズアプリ
- **技術スタック**: HTML / CSS / JavaScript(素のフロントエンド、フレームワーク不使用)

## 開発の進め方

- 外部ライブラリやビルドツールは導入せず、シンプルなHTML/CSS/JavaScriptで実装する
- ブラウザで `index.html` を直接開くか、簡易ローカルサーバーで動作確認する
- コードにコメントは最小限にし、変数・関数名で意図が伝わるようにする

## ディレクトリ構成(予定)

```
quiz-app/
├── index.html      # メイン画面
├── css/
│   └── style.css   # スタイル定義
├── js/
│   └── main.js      # クイズのロジック
└── CLAUDE.md
```

※ 実装が進み次第、実際の構成に合わせて更新してください。

## コーディング規約

- インデントは半角スペース2つ
- JavaScriptは基本的にモダンな構文(`const`/`let`、アロー関数など)を使用する
- クイズの問題データは配列やJSONとして分離し、ロジックと分けて管理する

## 応答について

- このプロジェクトに関するやり取りは日本語で行う
